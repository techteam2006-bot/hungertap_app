# Documentation
*.md

# Git metadata
.git/
.gitattributes
.github/

# Secrets — set EXPO_PUBLIC_* in EAS project env (Dashboard or `eas env:create`)
.env
.env.*
!.env.example

# Sentry token local overrides (keep android/sentry.properties uploaded for symbol upload)
android/sentry.properties.local
.sentryclirc
.env.sentry-build-plugin

# Dependencies & install artifacts (EAS runs npm ci on the server)
node_modules/
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local build outputs & measurement artifacts
build-artifacts/
dist/
web-build/
.expo/
.expo-shared/
android/app/build/
android/build/
android/.gradle/
android/local.properties
local.properties
*.apk
*.aab
*.bak

# Credentials & keystores — set in EAS Dashboard, never upload key files
build-credentials/
credentials.json
credentials/

# OS & Editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
