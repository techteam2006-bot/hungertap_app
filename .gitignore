# Dependencies & caches
node_modules/
.npm/
.expo/
web-build/
dist/

# Environment & secrets
.env*
!.env.example
credentials.json
credentials/
build-credentials/
# Keep android/sentry.properties committed (org/project only — never put auth.token in it).
# Local overrides with tokens: android/sentry.properties.local (ignored)
android/sentry.properties.local
.sentryclirc
.env.sentry-build-plugin

# Android / native build outputs
android/build/
android/app/build/
android/.gradle/
android/local.properties
build-artifacts/
*.apk
*.aab
*.bak
android/app/release/
android/app/build/outputs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log
yarn-error.log
