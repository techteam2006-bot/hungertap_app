# Dependencies & caches
node_modules/
.npm/
.expo/
web-build/
dist/

# Environment & secrets
.env*
!.env.example
credentials.json
credentials/
build-credentials/
# Keep android/sentry.properties committed (org/project only — never put auth.token in it).
# Local overrides with tokens: android/sentry.properties.local (ignored)
android/sentry.properties.local
android/keystore.properties
android/upload-keystore.jks
android/*.jks
.sentryclirc
.env.sentry-build-plugin

# Android / native build outputs
builds/
build-artifacts/
.gradle/
android/build/
android/app/build/
android/.gradle/
android/local.properties
*.apk
*.aab
*.bak
*.keystore
android/app/release/
android/app/build/outputs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log
yarn-error.log

# Supabase temp
.temp/
supabase/.temp/

# Local agent / deploy scratch (never commit)
.tmp_*
.b64_*
.mcp_*
.deploy_*
.split_*
tmp_*
*_chunk*.txt
*_content_only*
*_content_escaped*
*_deploy_payload*.json
.mcp_args_*.json
.mcp_split_*.json
.deploy_payload*.json
**/_deploy_payloads/
