<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Transparent nav bar so app chrome (tab bar) shows through; style set at runtime. -->
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#000000</item>
    <!-- Kill Android autofill yellow highlight (must be a Drawable, not a color) -->
    <item name="android:autofilledHighlight" tools:targetApi="26">@drawable/autofill_highlight</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
    <item name="android:autofilledHighlight" tools:targetApi="26">@drawable/autofill_highlight</item>
  </style>
</resources>
